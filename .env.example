# ── Multi-Tenant App Registration ──────────────────────────────
# Client ID from the multi-tenant app registered in your home tenant
CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Certificate for app-only auth (PEM format)
CERTIFICATE_PATH=/path/to/cert.pem
CERTIFICATE_THUMBPRINT=AABBCCDDEEFF00112233445566778899AABBCCDD

# ── Target Tenant ──────────────────────────────────────────────
# Set per collection run (or configure in a tenants.json manifest)
TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
AGENCY_ID=dept-of-education

# ── GCC Cloud Selection ────────────────────────────────────────
# Leave empty for M365 GCC standard (graph.microsoft.com)
# Set to "usgovernment" for GCC High (graph.microsoft.us)
NATIONAL_CLOUD=

# ── Azure Function App Ingestion Endpoint ──────────────────────
FUNCTION_APP_URL=https://purview-gov-func.azurewebsites.net/api/ingest
FUNCTION_APP_KEY=your-function-key-here

# ── mTLS Client Certificate for Function App ──────────────────
FUNCTION_APP_CERT_PATH=/path/to/mtls-client-cert.pem
FUNCTION_APP_CERT_KEY_PATH=/path/to/mtls-client-key.pem
